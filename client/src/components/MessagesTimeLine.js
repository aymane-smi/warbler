import React from 'react';

import MessagesList from './MessagesList';

const MessagesTimeLine = (props)=>{
    return (<div className="row flex-list">
        <MessagesList />
    </div>);
}

export default MessagesTimeLine;